<h1><%= link_to @group.name, event_group_path(@event, @group.id) %></h1> <br>

<p> <%= @group.description %> </p>

<ul>
  <% @group.users.each do |u| %>
    <li> <%= image_tag(u.avatar.thumb) %> </li>
    <% end %>
</ul>
