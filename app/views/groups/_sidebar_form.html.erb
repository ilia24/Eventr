<%= form_for @message, url: '#' do |f| %>
  <%= hidden_field_tag 'group_id', g.id %>
    <%= f.text_field :body, placeholder: 'Write Message', class: 'form-control' %>
    <small class="text-muted"></small>

<% end %>
