<%= form_for @message, url: '#' do |f| %>
  <%= hidden_field_tag 'group_id' %>
    <%= f.text_field :body, placeholder: 'Write Message', class: 'form-control', :autocomplete=> :off %>
    <small class="text-muted"></small>

<% end %>
